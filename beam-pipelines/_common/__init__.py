"""Shared helpers for every chapter pipeline.

Importing pattern used by chapters:

    import sys, os
    sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
    from _common.options import portable_options
"""
